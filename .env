# MÔI TRƯỜNG ỨNG DỤNG
# Đặt là 'development' khi phát triển, 'production' khi chạy thật
NODE_ENV=development

# CẤU HÌNH SERVER
PORT=8080

# CẤU HÌNH CORS
# Danh sách các domain được phép truy cập, phân cách bởi dấu phẩy
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8081

# CẤU HÌNH DATABASE (Microsoft SQL Server)
DB_HOST=localhost
DB_USERNAME=sa
DB_PASSWORD=sg6H625#51
DB_NAME=master
DB_PORT=1433         # ✅ Port mặc định của MS SQL Server

# CẤU HÌNH BẢO MẬT
# Chuỗi bí mật để tạo JSON Web Tokens (JWT). Tạo một chuỗi ngẫu nhiên, dài và phức tạp.
JWT_SECRET=

# CÁC DỊCH VỤ BÊN NGOÀI
# Ví dụ: API Key cho một dịch vụ gửi email
# SENDGRID_API_KEY=